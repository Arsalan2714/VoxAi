import 'dotenv/config'; import { graph } from './graph/graph.js'; const prompts=['hi','Tell me a joke','Explain recursion','What is the weather?','Translate this to French: hello']; async function run(){ for(const prompt of prompts){ try{ console.log('PROMPT:',prompt); const r=await graph.invoke({prompt, conversationId:'000000000000000000000000'}); console.log('OK', r.aiResponse); } catch(e){ console.error('ERROR for', prompt); console.error(e); console.error(e.stack); break; } } } run();
